class House {
    constructor(
      // Defines parameters:
      color,
      height,
      width,
      material,
    ) {
      // Define properties:
      this.color = color;
      this.height = height;
      this.width = width;
      this.material = material
      }
    }

export default House;